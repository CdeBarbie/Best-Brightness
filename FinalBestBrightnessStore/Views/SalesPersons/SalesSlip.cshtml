@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model FinalBestBrightnessStore.Models.SalesSlipViewModel

<!DOCTYPE html>
<html>
<head>
    <title>Sales Slip</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/0.4.1/html2canvas.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.3.2/jspdf.min.js"></script>
    <style>
        .container {
            margin-top: 20px;
        }

        .sales-slip-title {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .back-button {
            margin-top: 20px;
            display: flex;
            justify-content: flex-end;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="sales-slip-title">
            <h2>Sales Slip</h2>
            <button class="btn btn-primary" onclick="generatePdf()">Generate PDF</button>
        </div>
        <div class="row">
            <div class="col-md-6">
                <h4>Order Details</h4>
                <p><strong>Order ID:</strong> @Model.OrderId</p>
                <p><strong>Date:</strong> @Model.DateOfSale.ToString("dd/MM/yyyy")</p>
                <p><strong>Salesperson:</strong> @Model.SalesPersonName</p>
            </div>
            <div class="col-md-6">
                <h4>Payment Details</h4>
                <p><strong>Total Amount:</strong> @Model.TotalAmount.ToString("C")</p>
                @if (Model.PaymentType == "cash")
                {
                    <p><strong>Amount Paid:</strong> @Model.AmountPaid.ToString("C")</p>
                    <p><strong>Change:</strong> @Model.Change.ToString("C")</p>
                }
                else
                {
                    <p><strong>Payment Type:</strong> Card</p>
                }
            </div>
        </div>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.ProductName</td>
                        <td>@item.Quantity</td>
                        <td>@item.Price</td>
                        <td>@(item.Quantity * item.Price)</td>
                    </tr>
                }
            </tbody>
        </table>
        <div class="back-button">
            <button class="btn btn-secondary" onclick="window.location.href='/SalesPersons/SalespersonDashboard'">Back</button>
        </div>
    </div>

</body>
</html>
<script>
    function generatePdf() {
        html2canvas(document.body, {
            onrendered: function (canvas) {
                var img = canvas.toDataURL("image/png");
                var doc = new jsPDF();
                doc.addImage(img, 'JPEG', 40, 20);
                doc.save('sales-slip.pdf');
            }
        });
    }
</script>
@*<div class="container">
        <div class="sales-slip-title">
            <h2>Sales Slip</h2>
            <button class="btn btn-primary" onclick="generatePdf()">Generate PDF</button>
        </div>
        <div>
            <p>Salesperson: @Model.SalesPersonName (@Model.SalesPersonId)</p>
            <p>Date: @Model.Order.DateOfOrder.ToString("yyyy/MM/dd HH:mm")</p>
            <p>Amount Paid: @Model.AmountPaid</p>
            <p>Change: @Model.Change</p>
            <p>Total Amount: @Model.TotalAmount</p>
        </div>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th>Total Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Order.OrderItems)
                {
                    <tr>
                        <td>@item.Product.productName</td>
                        <td>@item.Quantity</td>
                        <td>@item.Price</td>
                        <td>@(item.Quantity * item.Price)</td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="back-button">
            <button class="btn btn-secondary" onclick="window.location.href='/SalesPersons/SalespersonDashboard'">Back</button>
        </div>
    </div>
    *@